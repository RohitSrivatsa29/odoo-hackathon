# Traveloop

A refined, high-end travel planning interface with an Elegant Dark aesthetic.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm start`
